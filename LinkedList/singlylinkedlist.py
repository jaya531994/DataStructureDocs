class Node:
    def __init__(self, value = None) -> None:
        self.value = value
        self.next = None

class SLingkedList:
    def __init__(self) -> None:
        self.head = None
        self.tail = None

SignleLingkedList = SLingkedList()
node1 = Node(1)
node2 = Node(2)

# SignleLingkedList.head = 

# Insertion of linked list in memory
